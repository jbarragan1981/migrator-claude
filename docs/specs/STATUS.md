# STATUS - tablero vivo

Actualizalo al empezar y al cerrar cada tarea. Los hooks leen las lineas "- [ ]" para recordar pendientes.

## M0 - Esqueleto
- [x] Crear estructura del monorepo segun CLAUDE.md 3 (uv workspace + pnpm en apps/web)
- [x] justfile con setup/inventory/convert/test/lint/api/web/up/gen-client
- [ ] CI en verde con paquetes vacios - workflow creado (.github/workflows/ci.yml); just setup/lint/test verificados localmente, falta confirmar la corrida real en GitHub Actions (no se hizo push todavia)
- [x] Comando inventory end-to-end: core (domain/ports/adapters/usecase) + CLI, contra fixtures sinteticos (spec 01, CA-1..CA-8)
- [x] Bloqueantes de la revision: inventario sin nombres de archivo hoja (ADR-0004, CA-7), .coverage ignorado, CLI atrapa ClaudeExportMdError (codigo 2), regex de sufijo de parte que ya no confunde fechas (CA-8)
- [x] Fase 0: correr inventario sobre la carpeta real y documentar las 5 categorias en docs/export-format/ (conversations.md, memories.md, projects.md, frames.md, light_metadata.md - las 5 escritas contra docs/export-format/inventory.json y los fixtures anonimizados existentes)
- [x] Fixtures anonimizados por categoria, incluido manifest (packages/core/tests/fixtures/v2026-batched/{conversations,memories,projects,frames,light_metadata,manifest}/) - generados y regenerados dos veces con scripts/extract_real_samples.py a partir del export real; el fixture de manifest NO va en modo estructura (category/part/batch_index/filename/total_files/version/instructions quedan literales por ser esquema de Anthropic, no datos del usuario - solo se quita export_url)

## M1 - memories + conversations (CERRADO, con los pendientes anotados en cada linea)
- [x] Modelo de dominio (spec 02) - domain/entities.py (Account, Conversation, Message, ContentBlock, Project, ProjectDoc, Memory, Frame/FrameVersion, Report, ParseError) + domain/dates.py; Memory.project_id y rutas sinteticas decididas en ADR-0005; spec 02 actualizado. Falta Export, se decide al escribir usecases/convert.py
- [x] Parser memories + render + snapshot - UN SOLO archivo JSON por cuenta (spec 03 CA memories-1..5 y comunes CA-C1..C5, cobertura test a test anotada en 03-parsers.md). Incluye la infraestructura de rendering que reusaran los proximos parsers: rendering/{filters,render}.py + templates/memory.md.j2 (Jinja2, frontmatter emitido con el dumper de PyYAML) y adapters/sink_filesystem.py (escribe output/memories/<slug>.md, UTF-8 y LF siempre). Snapshot con syrupy en tests/__snapshots__/. Conectado al CLI convert (ver linea de abajo)
- [x] Parser conversations (streaming, ~100MB) + render + artefactos (spec 03 CA conversations-1..5 y comunes CA-C1..C5, detalle test a test en 03-parsers.md). adapters/parsers/conversations.py con ijson real (nunca json.load); render en rendering/conversations.py + rendering/artifacts.py (extrae cercas de codigo >40 lineas a artifacts/NN-<nombre>.<ext>) + templates/conversation.md.j2; escritura en sink_filesystem.py::write_conversations. Orden deterministico via parse_sorted() (creado at, luego id). _index.md global queda pendiente para cuando este el CLI convert armando el arbol completo (proxima tarea)
- [x] CLI convert usable end-to-end (spec 05) - usecases/convert.py (orquesta parse->render->write de memories + conversations con un unico Report) + rendering/index.py con templates/{index,readme}.md.j2 + sink_filesystem.{ensure_writable,write_report} + sink_json.{dumps_summary,dumps_errors} + comando apps/cli/.../commands/convert.py registrado en main.py. Arbol generado: README.md, _index.md, memories/**, conversations/YYYY/MM/** (con artifacts/), _report/{summary.json,errors.jsonl,inventory.json}. Tests: packages/core/tests/test_convert.py (35), packages/core/tests/test_render_index.py (9, con snapshot syrupy del _index.md y del README.md) y apps/cli/tests/test_convert_cmd.py (10). PENDIENTE de spec 05: flags --only, --strict (codigo 3) y --json de convert, documentados con su motivo en 05-cli.md seccion "Estado de convert" (renombrada a "(M2)" al cerrar M2). PENDIENTE de CLAUDE.md 5: projects/, frames/ y account/ (M2) - no se crean carpetas vacias y el README generado lo dice.

- [x] Bloqueantes de la revision de M1 (architect-reviewer), los 3 + la observacion relacionada + el detalle de documentacion:
  1. **Puerto MarkdownSink** (CLAUDE.md 3, ADR-0006) - ports/sink.py con `location` / `ensure_writable(overwrite)` / `write(path, content)` (rutas relativas POSIX, contenido ya renderizado). adapters/sink_filesystem.py pasa a ser `FilesystemSink` y pierde las funciones que mezclaban render con escritura; el bucle `render -> sink.write` vive ahora en usecases/convert.py. `convert(source, sink, ...)` recibe el puerto, `ConvertResult.out` -> `destination: str` y `files` -> rutas relativas POSIX; el CLI construye el `FilesystemSink(salida)`. Test con sink en memoria (`RecordingSink` en test_convert.py) que prueba la orquestacion sin tocar disco. NO se creo `ProgressReporter`: el callback `(categoria, hechos)` sigue siendo un `Callable` hasta que la API (M3) pida una forma real (motivo en ADR-0006).
  2. **Poda de URLs recursiva** - `domain/manifest.py::prune_urls` quita las claves con "url" a cualquier profundidad (dicts y listas anidadas); la usan `ManifestFile._normalise` y `adapters/manifest.py::_root_extras`. Antes `{"delivery": {"url": ...}}` sobrevivia en `Manifest.extra` y de ahi a `_report/inventory.json`. Test de regresion: test_manifest.py::test_urls_are_pruned_at_every_depth_not_only_at_the_root.
  3. **Filtros hhmm y code_fence registrados** - `rendering/render.py::FILTERS` es ahora la unica lista y `build_environment` la aplica entera; con StrictUndefined una plantilla de usuario que usara `{{ x | hhmm }}` reventaba con TemplateAssertionError pese a estar documentada en spec 04. Tests en test_render_memories.py (los 8 filtros de la tabla + render real de hhmm/code_fence).
  4. **Colision de rutas de conversacion** (observacion) - `rendering/conversations.py::ConversationPaths` desempata en streaming (la primera se queda la ruta, las siguientes llevan el hash corto de su `id`, mas un contador si hasta el uuid estaba repetido) Y deja una advertencia en `Report.warnings` -> `_report/summary.json`. Antes la segunda pisaba a la primera en silencio. El `_index.md` enlaza la ruta ya desempatada. Tests en test_render_conversations.py (5) y test_convert.py (4, end-to-end con dos uuids gemelos).
  5. **Documentacion que mentia** - el comentario de `PROJECT_MEMORY_DIR` en domain/entities.py y el punto 2 de ADR-0005 decian que el prefijo `_` evitaba la sobrescritura; no es cierto (slugify lo quita) y quien protege de verdad es `assign_output_paths`. Corregidos ambos (el ADR con una nota de correccion, sin reescribir la decision original).

- [x] adapters/manifest.py conserva ahora las claves RAIZ del manifiesto que no son la lista de archivos (`created_at`, `version`, `instructions`...) en `Manifest.extra`, salvo cualquier clave con "url" (siguen descartandose siempre). Antes se perdian en silencio; `created_at` es la fecha en que Anthropic genero el export y es la unica fecha que el README de la salida puede mostrar sin mirar el reloj (CLAUDE.md 1.4). No toca domain/ ni ports/ (Manifest ya era extra="allow"), asi que no necesita ADR.

## M2 - projects, light_metadata, frames, indices (CERRADO, con los pendientes anotados en cada linea)
- [x] Parser light_metadata + render - mapea directo a Account (spec 03 CA light_metadata-1..3 y comunes CA-C1..C5, detalle test a test en 03-parsers.md). adapters/parsers/light_metadata.py (json.load con techo MAX_JSON_BYTES=10MB: el archivo real pesa 152 bytes, el streaming de CA-C3 es para conversations) + render en rendering/render.py (account_title/account_output_path/assign_account_paths/account_path_warnings/render_account/render_accounts, junto al de memories porque las dos entidades son frontmatter mas un cuerpo corto) + templates/account.md.j2. Salida: account/account.md, la UNICA ruta fija del arbol (CLAUDE.md 5). Tests: test_parse_light_metadata.py (22) y test_render_light_metadata.py (20, con snapshot syrupy), 100% de linea en el parser y en rendering/render.py. Decisiones propias: (a) varias cuentas -> NINGUNA se queda account/account.md, todas pasan a account/account-<hash8 del id>.md + warning en Report (el export real solo trae 1, pero CA-3 prohibe asumirlo); (b) raiz que es un objeto suelto en vez de array -> se trata como array de 1 con warning, para no devolver cero cuentas en silencio; (c) campos con nombre de credencial (token/secret/key/password/auth/cookie/session/...) -> se conserva la clave y se sustituye el VALOR por un marcador + warning, mientras que verified_phone_number se conserva entero por ser dato de perfil (CA-2); domain/redaction.py NO se reuso: resuelve otro problema (nombres de archivo del inventario, ADR-0004). CONECTADO a usecases/convert.py en la ronda final de M2 (ver la linea de cierre): account/account.md se escribe end-to-end, las advertencias del parser llegan al Report de la corrida y el _index.md tiene su seccion Cuenta. No se toco domain/ ni ports/, asi que no hizo falta ADR.
- [x] Parser projects + render - un archivo JSON por proyecto, no un array unico (spec 03 CA projects-1..5 y comunes CA-C1..C5, detalle test a test en 03-parsers.md). adapters/parsers/projects.py (json.load con techo MAX_JSON_BYTES=10MB: los 32 archivos reales suman 22 KB; parse() en el orden de los archivos + parse_sorted()/sort_key() para el orden canonico) + render en rendering/render.py (project_title/project_dir/assign_project_dirs/project_output_path/project_doc_paths/project_context/project_doc_context/render_project/render_projects) + templates/project.md.j2 y project_doc.md.j2. Salida: projects/<slug-proyecto>/project.md y projects/<slug-proyecto>/docs/<slug-doc>.md (CLAUDE.md 5); cada doc es un archivo propio (type: project_doc) para que el dia que aparezca su contenido solo haya que llenarlo. Tests: test_parse_projects.py (37) y test_render_projects.py (29, con snapshot syrupy), 100% de linea en el parser y en rendering/render.py. Decisiones propias: (a) docs[] sin contenido -> ProjectDoc.content=None (ADR-0005), UNA advertencia agregada por corrida en report.warnings (DOCS_WITHOUT_CONTENT_WARNING, contada en parse() despues del dedupe; era una por doc hasta la revision de M2) y el .md de cada doc lo DICE con DOC_CONTENT_UNAVAILABLE, nunca vacio en silencio ni inventado; (b) conversation_ids queda VACIO a proposito: el archivo del proyecto no lista sus conversaciones, el enlace va al reves desde conversations[].project_uuid y lo resolvera usecases/convert.py (el render ya lo soporta y mientras tanto dice que ninguna conversacion apunta al proyecto); (c) prompt_template="" se conserva como instrucciones vacias y la seccion lo dice, solo la clave ausente omite la seccion; (d) creator es objeto -> creator_id=creator.uuid y el full_name se conserva en extra.creator sin duplicar el uuid, y si creator llegara con otra forma queda en extra.creator_raw con aviso; (e) doc sin uuid -> id sintetico <uuid del proyecto>#docs[i] + extra.synthetic_id + aviso, para no perder el filename; (f) raiz que es un array (formato legacy) -> se leen todos sus items con aviso, para no devolver cero en silencio; (g) colision de slugs entre proyectos o entre docs de un proyecto -> AMBOS llevan el hash corto de su id, como en memories. CONECTADO a usecases/convert.py en la ronda final de M2 (ver la linea de cierre): projects/ se escribe end-to-end y conversation_ids lo puebla usecases/links.py cruzando conversations[].project_uuid (CA-4). No se toco domain/ ni ports/, asi que no hizo falta ADR.
- [x] Parser frames + render - un archivo JSON por artefacto en frames-000/artifacts/<uuid>/ (spec 03 CA frames-1..3 y comunes CA-C1..C5, detalle test a test en 03-parsers.md). adapters/parsers/frames.py (json.load con techo MAX_JSON_BYTES=10MB: los 17 archivos reales suman 11 KB; parse() en el orden de los archivos + parse_sorted()/sort_key() para el orden canonico) + render en rendering/render.py (frame_title/frame_created_at/frame_tags/frame_output_path/assign_frame_paths/frame_context/render_frame/render_frames) + templates/frame.md.j2. Salida: frames/<slug>.md, PLANO (CLAUDE.md 5 dejaba la categoria "por investigar"; la Fase 0 la resolvio y un artefacto es UN archivo, no tiene hijos que colgar como los docs de un proyecto); el slug sale del titulo de la version activa y, si no hay titulos, del kind. Tests: test_parse_frames.py (34) y test_render_frames.py (29, con snapshot syrupy), 100% de linea en el parser y en rendering/render.py. Decisiones propias: (a) CA-2, el contenido real del artefacto NO esta en el export -> Frame.payload conserva el JSON COMPLETO tal cual, UNA advertencia agregada por corrida en report.warnings (FRAMES_WITHOUT_CONTENT_WARNING, contada en parse() despues del dedupe; era una por artefacto hasta la revision de M2) y el .md de cada artefacto lo DICE con FRAME_CONTENT_UNAVAILABLE (mismo criterio y misma forma que DOC_CONTENT_UNAVAILABLE de projects.docs[]) ademas de publicar el JSON integro en un <details>, nunca un bloque vacio en silencio; (b) payload y extra conviven a proposito (extra = solo lo desconocido, que es lo que publica el frontmatter; payload = el seguro de CA-2), duplicar aqui cuesta 11 KB, no los 98 MB de conversations; (c) ORDEN SIN created_at: el artefacto no trae fecha de creacion propia, asi que sort_key usa updated_at y luego id, y la fecha de creacion del frontmatter se DERIVA de la version mas antigua de versions[] (frame_created_at), null si ninguna trae fecha - dato del export, nunca el reloj de la corrida; (d) CA-3, active_version que no coincide con ningun versions[].id -> warning y Frame.active()=None, el artefacto se emite igual y el .md lo cuenta con ACTIVE_VERSION_UNMATCHED_NOTE en vez de callar la incoherencia; (e) version sin id -> id sintetico <id del artefacto>#versions[i] + extra.synthetic_id + aviso, para no perder su titulo/descripcion; (f) active_version con forma inesperada -> extra.active_version_raw con aviso; (g) raiz que es un array -> se leen todos sus items con aviso, como en projects; (h) colision de slugs -> AMBOS llevan el hash corto de su id, como en memories y projects. CONECTADO a usecases/convert.py en la ronda final de M2 (ver la linea de cierre): frames/ se escribe end-to-end, en orden canonico por updated_at, y tiene su seccion Artefactos en el _index.md. No se toco domain/ ni ports/ (Frame, FrameVersion y Frame.active() ya existian desde M1), asi que no hizo falta ADR.
- [x] Cierre de M2: las 5 categorias conectadas a convert + enlace conversacion<->proyecto + _index.md ampliado. usecases/links.py (ProjectLinker/ResolvedProject) es el UNICO sitio donde se cruzan las dos colecciones (spec 03 projects CA-4), sobre el vocabulario puro de rendering/links.py (ProjectLink, ConversationLink, relative_link con posixpath, day, date_sort_key) que quedo escrito de la ronda anterior y se uso TAL CUAL. usecases/convert.py orquesta ahora memories -> account -> (parse projects) -> conversations (streaming) -> projects -> frames -> _index/README/_report: los proyectos se parsean ANTES de las conversaciones (para que cada conversacion nombre el suyo mientras se escribe) y se escriben DESPUES (para enumerar las que apuntan a cada uno); CONVERTED_CATEGORIES y summary.json.categories llevan las 5. Salida: account/account.md, projects/<slug>/{project.md,docs/*.md}, frames/<slug>.md, ademas de memories/ y conversations/ de M1. _index.md tiene ahora cinco secciones (Conversaciones, Proyectos, Memorias, Artefactos, Cuenta); la columna Proyecto de conversaciones Y de memorias muestra el NOMBRE con enlace relativo cuando resuelve y el uuid crudo cuando no (sin enlace roto). El readme.md.j2 ya no promete categorias futuras: enumera solo las carpetas que ese export produjo (condicionado a counts) y dice que el export no trae el contenido de los docs de proyecto ni de los artefactos. Tests: test_links.py (24, nuevo), test_convert.py (+20, de 35 a 55: seccion "las cinco categorias" con fixture de las 5 juntas, conversacion huerfana, dos cuentas, determinismo) y test_render_index.py (+5, de 9 a 14, con el snapshot ampliado a las 5 secciones). 585 tests en verde, cobertura de core 99.36% (links.py, index.py y convert.py al 100%). Decisiones: (a) el cruce NO va en un parser ni en un adapter porque necesita las dos colecciones a la vez - es orquestacion; (b) proyecto no encontrado -> se conserva el uuid crudo sin enlace y queda UNA advertencia agregada por corrida (un export sin la categoria projects produciria cientos de lineas identicas); (c) el project.md enumera sus conversaciones en tabla (titulo, enlace relativo, fecha) cuando el cruce las trae y cae a los uuids de Project.conversation_ids si alguien renderiza el proyecto suelto - el contrato viejo del render sigue vivo; (d) las memorias de proyecto (ADR-0005) se cruzan igual que las conversaciones en el indice; (e) IndexEntry gano campos opcionales (project, doc_count, conversation_count, kind, content_available) en vez de tener un tipo de fila por categoria: el orden y el escapado son los mismos para todas. No se toco domain/ ni ports/, asi que no hizo falta ADR (Project.conversation_ids ya existia y se puebla con model_copy, sin mutar la entidad del parser).
- [x] Revision de M2 (architect-reviewer): los 2 bugs de comportamiento + los 2 detalles menores, sin bloqueantes de arquitectura.
  1. **Advertencia emitida antes del dedupe** - frames.py y projects.py avisaban del "contenido no disponible" DENTRO de _frame/_doc, o sea ANTES de que parse() descartara el item repetido por id (CA-C1): dos partes con el mismo id escribian 1 archivo pero dejaban 2 avisos, y el informe nombraba archivos que nunca se escribieron. El conteo vive ahora en parse(), sobre lo que de verdad se emite. Tests de regresion: test_parse_frames.py::test_ca2_the_aggregated_warning_only_counts_the_frames_that_survived_the_dedupe y test_parse_projects.py::test_ca3_the_aggregated_warning_only_counts_the_docs_that_survived_the_dedupe (el caso exacto: dos partes, mismo id).
  2. **El limite de avisos del CLI escondia lo accionable** - con 8 proyectos el resumen mostraba 3 veces el mismo "documento sin contenido" y tapaba tras "...y 6 mas" al aviso (unico) de proyectos huerfanos y al de credenciales. Arreglado en los DOS extremos: (a) en el core, projects y frames agrupan ahora su aviso de contenido en UNA linea por corrida ("8 documentos de 8 proyectos llegan solo con su metadata; el contenido no esta en este export (cada documento lo dice en su propio archivo)"), mismo criterio que ORPHAN_PROJECTS_WARNING - spec 03 (projects CA-3 y frames CA-2) actualizado para decir lo mismo que el codigo; (b) en el CLI, convert.py::group_warnings agrupa por FAMILIA (el mensaje sin rutas ni identificadores) y ordena las familias de menos a mas repetidas, asi los avisos que salen una sola vez van delante, y warning_summary trunca por familia y cuenta "... y N tipos de aviso mas". Tests: apps/cli/tests/test_convert_cmd.py (+10, de 10 a 20, incluido el end-to-end con 8 proyectos + conversacion huerfana).
  3. **Detalle: docstring de project_doc.md.j2** decia que el titulo era el filename "sin extension" y project_doc_title devuelve el filename COMPLETO (lo que ya decian render.py y spec 04). Corregido el comentario de la plantilla.
  4. **Detalle: ORPHAN_PROJECTS_WARNING sin singularizar** ("2 conversaciones apuntan a 1 proyectos"). Ahora concuerda en numero y verbo, con el mismo criterio de plural que ya usaba el CLI; test_links.py::test_the_orphan_warning_is_written_in_singular_when_there_is_only_one. Suite completa: 598 tests en verde (core+cli+api), cobertura de core 99.40% (projects.py, frames.py y usecases/links.py al 100%).
- [ ] PENDIENTE de M2 que pasa a M3+: flags --only, --strict (codigo 3) y --json de convert (motivo actualizado en 05-cli.md, seccion "Estado de convert (M2)"); sigue sin poder verificarse el enlace conversacion<->proyecto con el fixture REAL anonimizado (ningun item de la muestra de conversations tiene project_uuid no-null y el anonimizador rompe la integridad referencial: misma deuda tecnica de --keep-structure-only ya anotada abajo). El contenido real de los docs de proyecto y de los artefactos sigue sin aparecer en el export (dos decisiones pendientes del usuario, abajo).

## M3 - API + jobs + SSE + Docker (EN CURSO)
- [x] POST /exports (modo local {"path"}, gateado por CEM_ALLOW_LOCAL_PATHS) + GET /exports/{id}/inventory (spec 06 CA-1, CA-9). apps/api/src/claude_export_md_api/{errors.py,main.py,schemas/{problem.py,exports.py},services/exports.py,routers/exports.py}. `ExportStore` (services/exports.py) es un dict `export_id -> ExportRecord(path, owned)` protegido por `threading.Lock`, en memoria y modulo-singleton (`get_store()`); el `JobStore` real de `/convert`/`/jobs` es la proxima ronda, esto solo recuerda que carpeta corresponde a que id. `CEM_ALLOW_LOCAL_PATHS` (default `false`, se lee con `os.environ.get(...).strip().lower() == "true"` en cada request, nunca cacheado a nivel de import): con la bandera apagada, `{"path": "..."}` devuelve 403; encendida, se valida `Path.exists()` + `os.access(path, os.R_OK)` antes de registrar (ruta inexistente o sin permisos -> 400). El `export_url` de un manifiesto NUNCA se lee ni se usa (CLAUDE.md 5), ni siquiera si el cliente lo manda en el body (test de regresion). `GET /exports/{id}/inventory` delega el 100% de la logica en core (`create_source(directory)` + `build_inventory(source)`) y devuelve `Inventory` (el `BaseModel` de core) directo como `response_model`, sin duplicar su forma en un schema propio; id inexistente -> 404, `ClaudeExportMdError`/subclases (`UnknownFormatError`, `CorruptFileError`) de core -> 422 (decision propia: 400 es "tu request esta mal formado", 422 es "el request esta bien pero el contenido que senala no se puede procesar", que es exactamente lo que pasa aca). Router (`routers/exports.py`) decide el "modo" mirando `content-type`: `multipart/form-data` -> subida; cualquier otro -> se interpreta el body como JSON `{"path"}` (`request.json()`/`LocalExportRequest.model_validate`, ambos con su propio catch a 400) - no se pueden declarar los dos modos como parametros FastAPI en el mismo endpoint (uno pide `UploadFile`, el otro un modelo pydantic; FastAPI infiere el tipo de body por los parametros y no permite mezclarlos), asi que el endpoint toma el `Request` crudo y bifurca a mano; costo: el OpenAPI de este endpoint no describe el body todavia (revisar cuando haga falta el cliente Angular real, `just gen-client` no se corrio en esta ronda porque no se toco apps/web). Errores RFC 7807: `errors.py::ProblemError` + un unico exception handler en `main.py` (tambien cubre `StarletteHTTPException` y `RequestValidationError`) que arma `{type, title, status, detail}` con `media_type="application/problem+json"` - ningun router construye el JSON de error a mano. Tests: apps/api/tests/{test_exports_local.py (6), test_exports_inventory.py (3), test_no_network.py (1)} + apps/api/tests/conftest.py (fixture autouse `_isolated_work_dir` que fija `CEM_WORK_DIR` al `tmp_path` de cada test) y apps/api/tests/__init__.py (necesario: sin el, el `conftest.py` nuevo de apps/api/tests colisiona por nombre bare-module con packages/core/tests/conftest.py bajo pytest en modo "prepend" cuando se corren los tres paquetes juntos - con `__init__.py` pytest lo nombra `tests.conftest` en vez de `conftest` y deja de pisar al de core). El test de "sin red" (CA-8) no bloquea `socket.socket` completo (en Windows el propio `TestClient` necesita crear un socket real de loopback para el `BlockingPortal` de anyio, al no haber `socketpair` nativo) sino `socket.socket.connect`/`connect_ex` hacia cualquier host que no sea loopback, ejercitando health + los tres modos de `/exports` + `/inventory` de punta a punta.
- [x] POST /exports multipart (zips/manifiesto) con limite CEM_MAX_UPLOAD_MB y validacion anti path-traversal (spec 06 CA-1, CA-5). Mismos archivos que la linea de arriba (`services/exports.py::register_upload`). Nombres de archivo: se aceptan solo archivos SUELTOS (CLAUDE.md 9 - una subida multipart siempre son zips individuales y/o el manifiesto, nunca una carpeta con estructura), asi que cualquier nombre con `/`, `\` o literal `.`/`..` se rechaza (400) ANTES de crear el directorio de trabajo o escribir un solo byte (test de regresion: verifica que `CEM_WORK_DIR` sigue vacio despues del rechazo). Limite de tamano (`CEM_MAX_UPLOAD_MB`, default 2048) en DOS capas, documentando la limitacion real de esta version de Starlette/FastAPI (se investigo antes de escribir codigo, ver debajo): (a) si el cliente manda `Content-Length`, se rechaza con 413 antes de tocar el body; (b) SIEMPRE ademas, cada archivo se copia a su destino en chunks de 1 MiB (`upload.read(chunk)`/`out.write(chunk)`) contando bytes acumulados, y se corta con 413 apenas se supera el limite - nunca se junta el archivo completo en un `bytes` de Python. Limitacion documentada a proposito (no se resolvio en esta ronda por alcance): esta version instalada de Starlette (`request.form(max_part_size=...)`) SOLO aplica ese limite a campos de formulario comunes, no a partes con `filename` (se leyo el codigo fuente de `starlette.formparsers.MultiPartParser.on_part_data`: el chequeo de `max_part_size` esta en la rama `self._current_part.file is None`, nunca en la rama de archivo) - o sea que FastAPI ya recibe la subida completa en el `SpooledTemporaryFile` de su `UploadFile` (spillado a disco, nunca en RAM como un solo objeto, pero si escrito a disco por Starlette) antes de que nuestro codigo pueda intervenir; el pre-chequeo de `Content-Length` cubre al cliente bien portado (el caso comun), y en produccion la mitigacion real para un cliente que miente sobre el tamano es un limite de body en el reverse proxy (nginx `client_max_body_size` u equivalente), fuera del alcance de esta app. `CEM_WORK_DIR` (default: `tempfile.gettempdir()`) nunca cae dentro del repo; cada export subido vive en `<CEM_WORK_DIR>/claude-export-md-<uuid>/` y se borra completo (`shutil.rmtree`) si la subida se aborta a mitad de camino (nombre invalido descubierto tarde, limite superado en streaming, cualquier excepcion). Tests: apps/api/tests/test_exports_upload.py (8): subida valida -> 201, subida valida inventariada de punta a punta (zip legacy con conversations/projects/users.json en la raiz -> `format_version == "legacy-single-zip"`), varios archivos en una subida (manifiesto + zip), `../../etc/passwd` y `..\..\windows\win.ini` -> 400 sin escribir nada, subida de 2 MiB con `CEM_MAX_UPLOAD_MB=1` -> 413, subida vacia -> 400/415/422. Dependencia nueva: `python-multipart>=0.0.9` en apps/api/pyproject.toml (`request.form()` de Starlette la requiere; no estaba declarada y el primer `pytest` fallo con `AssertionError: The python-multipart library must be installed`).
- [x] POST /exports/{id}/convert + JobStore en memoria (asyncio) + GET /jobs/{id} + GET /jobs/{id}/events (SSE, reusa el callback progress de convert() que ya emite por categoria - spec 06 CA-2, CA-3, CA-6). apps/api/src/claude_export_md_api/{schemas/jobs.py,services/jobs.py,routers/jobs.py}, registrado en main.py. **Donde queda la salida de un job**: `services/exports.py::output_directory(export_id)` (nuevo, junto a `export_directory`) devuelve `<directorio-de-trabajo-del-export>/_output/` - SIEMPRE dentro del directorio que ya tiene el export (local: la ruta del usuario; subida: `<CEM_WORK_DIR>/claude-export-md-<uuid>/`), nunca en otro lado, para que las rondas de `/output/tree`, `/output/file` y `/download` lo encuentren solo a partir del `export_id` sin guardar una ruta aparte. `FilesystemSink` sobre esa carpeta, `overwrite=True` siempre (un mismo export puede convertirse mas de una vez; no es la carpeta del usuario, es nuestra, se puede pisar). **`JobStore`** (services/jobs.py): mismo espiritu que `ExportStore` - dict `job_id -> _JobState` protegido por `threading.Lock`, modulo-singleton (`get_job_store()`), con `create/mark_running/append_progress/mark_done/mark_failed/get` (nombres que no atan la implementacion a "es un dict", para poder pasar a SQLite despues). Guarda `status` (`pending|running|done|failed`), la lista de eventos de progreso recibidos, conteos/errores/warnings/destino cuando termina, y `reason` cuando falla (incluido `"timeout"`, CA-6). **`convert()` de core es sincrona**: correrla sin bloquear el event loop usa `asyncio.wait_for(asyncio.to_thread(run_sync), timeout=...)`; el callback `progress` que le pasamos a `convert()` (`on_progress`) se invoca DESDE ESE thread (nunca desde el hilo del event loop), por eso `JobStore` necesita el lock igual que `ExportStore`. **Timeout** (`CEM_JOB_TIMEOUT_S`, default 300s, se lee en cada `start_conversion` - no cacheado): `asyncio.wait_for` cancela la TAREA que esperaba el resultado y el job queda `failed`/`"timeout"` de inmediato; el thread de Python que esta corriendo `convert()` NO se puede matar a la fuerza (limitacion del lenguaje) y puede seguir escribiendo en `_output/` hasta terminar solo, pero ya nadie lee ese resultado ni revierte el estado ya marcado - limitacion documentada a proposito, igual de real en cualquier framework sobre CPython. `start_conversion` valida el `export_id` (404 si no existe), acepta `only`/`templates` del body (`ConvertRequest`): `templates` se pasa tal cual a `convert()` (la firma de core ya lo acepta); `only` se acepta para no romper el contrato del spec pero SE IGNORA (deja un `logger.warning`) porque `usecases.convert.convert()` de core todavia no filtra categorias - mismo pendiente que `--only` del CLI (docs/specs/05-cli.md). La tarea de fondo se crea con `asyncio.create_task` y se guarda en un `set` modulo-nivel (`_background_tasks`) con `add_done_callback` para descartarla sola: sin esa referencia fuerte, `asyncio` puede recolectar la tarea a mitad de camino. **SSE** (`GET /jobs/{id}/events`): el router chequea `ensure_job_exists` (404 normal, `application/problem+json`) ANTES de construir el `StreamingResponse` - un 404 lanzado desde DENTRO de un generador ya en curso llega tarde, con la respuesta ya empezada. El generador (`stream_job_events`) hace poll simple sobre el `JobStore` cada 50ms (mas simple que coordinar `asyncio.Event`/`Condition` entre el thread de conversion y el loop, y barato a esta escala: son listas/dicts en memoria) y por cada vuelta manda los eventos `progress` nuevos que encuentre; si el job ya esta en `done`/`failed` manda ESE evento final y corta (no espera eventos que ya pasaron); si `request.is_disconnected()` corta sin mandar nada mas (la conversion sigue sola en segundo plano - lo unico que no puede pasar es el generador quedando vivo para siempre sin que nadie lo lea). **Bug de test descubierto y documentado**: `TestClient` de esta version de Starlette/httpx solo mantiene vivo su "portal" (el hilo con el event loop de la app) mientras se lo usa como context manager (`with TestClient(app) as client:`); sin el `with`, cada llamada (`client.post(...)`, `client.get(...)`) abre y cierra su PROPIO portal, y una tarea de `asyncio.create_task` lanzada durante un request que todavia no termino cuando la respuesta ya se mando queda huerfana para siempre (el thread de `asyncio.to_thread` sigue corriendo y de hecho termina `convert()`, pero la corrutina que esperaba ese resultado nunca se resume porque su event loop ya se cerro) - se investigo con un script standalone antes de asumir que era un bug propio; los tests de este archivo por eso comparten un unico `client` de punta a punta via fixture. Tests: apps/api/tests/test_convert_jobs.py (10): 202 en <200ms con la conversion deliberadamente lenta (`monkeypatch` sobre `jobs_service.run_convert`), job llega a `done` con los conteos/errores/warnings reales del fixture sintetico completo (`{"conversations": 2, "projects": 2}`, 3 errores, 5 warnings - el fixture tiene entradas invalidas a proposito en memories/light_metadata/frames), `GET /jobs/{id}` en pending/running/done, SSE con progreso + `done` final, SSE contra un job ya terminado (responde de una, <2s), timeout con `CEM_JOB_TIMEOUT_S=0` -> `failed`/`"timeout"`, `only` aceptado pero sin efecto, 404 de `export_id`/`job_id` inexistente en las tres rutas nuevas. Usa SIEMPRE una COPIA del fixture sintetico en `tmp_path` (nunca el `synthetic_export_dir` real del repo): a diferencia de `/inventory` (solo lectura), `/convert` escribe `_output/` dentro del directorio del export.
- [x] GET /exports/{id}/output/tree + GET /exports/{id}/output/file + GET /exports/{id}/download + DELETE /exports/{id} (spec 06 CA-4). **NOTA: la descripcion de `/download` en esta linea quedo OBSOLETA, describe el mecanismo de streaming con hilo+cola que las rondas 3-5 de revision de M3 reemplazaron por completo - ver la linea de cierre de M3 mas abajo para el diseno actual (zip completo a un temporal + TempZipResponse).** apps/api/src/claude_export_md_api/{schemas/output.py,services/output.py,services/lifecycle.py,routers/output.py}, routers/exports.py (agrega el DELETE), main.py (registra output_router). Los tres GET leen EXCLUSIVAMENTE de `exports_service.output_directory(export_id)` (la carpeta que `POST /convert` ya escribio); ninguno sabe de parseo mas alla de eso. **"Export sin convertir todavia" -> 404** en `/output/tree`, `/output/file` y `/download` (`services/output.py::require_output_dir`), con un `detail` que lo distingue del 404 de `export_id` inexistente - decision propia (spec 06 no lo aclara): desde el punto de vista del cliente `_output/` es un recurso mas bajo `exports/{id}` (como `/inventory`) y "todavia no existe" es justo lo que 404 comunica; 409 se reservo para un conflicto de ESTADO sobre un recurso que SI existe, que no es el caso. **`/output/tree`** devuelve una LISTA PLANA (no arbol anidado) de `{path, kind, size}` con `os.walk` ordenado (recorrido y `sorted()` final deterministicos, CLAUDE.md 1.4); `size=None` para `kind="dir"`. **CA-4 (path traversal) en `/output/file?path=`, en dos capas**: `_validate_relative_path` rechaza sobre el STRING crudo, ANTES de tocar el filesystem, cualquier ruta absoluta o componente `.`/`..`/vacio, usando `PureWindowsPath` (entiende `/` y `\` como separador sin importar el SO donde corre la API, asi que `..\..\windows\win.ini` se detecta igual en Windows y en Linux/CI, sin depender de en que plataforma corra el proceso); ya con la ruta candidata construida, `resolve()` + `relative_to()` la reconfirman descendiente de `_output/` (defensa en profundidad por symlinks). Archivo sintacticamente valido pero inexistente -> 404. `media_type` via `mimetypes.guess_type` con fallback a `text/markdown; charset=utf-8` para `.md` (mimetypes SI reconoce `.md` en esta instalacion, pero el fallback documentado cubre el caso de que no lo reconociera en otra). **`/download` arma el zip con STREAMING REAL** (no un archivo temporal completo ni todo en memoria antes de responder): `_QueueWriterStream` es un file-like minimo (`write/tell/flush/close`, deliberadamente SIN `seek` para que `zipfile.ZipFile` detecte automaticamente que no es seekable y use data descriptors) sobre el que escribe `zipfile.ZipFile`; un `threading.Thread` de fondo corre esa escritura empujando cada chunk a una `queue.Queue(maxsize=8)` (backpressure real: si el cliente lee lento, el hilo se bloquea en `put()` en vez de acumular memoria sin limite) mientras el generador async de `stream_zip` va sacando chunks de la cola con `asyncio.to_thread(queue.get)` a medida que existen. `require_output_dir` se llama en el ROUTER, ANTES de construir el `StreamingResponse` (no dentro del generador): un 404 lanzado desde dentro de un generador ya en curso llega tarde, con los headers (200) ya enviados - mismo criterio que `routers/jobs.py::get_job_events` con el SSE. **`DELETE /exports/{id}`** vive en `services/lifecycle.py` (no en `exports.py`, para evitar un ciclo: `jobs.py` ya importa `exports.py`) y su decision central es el MATIZ local vs subida: modo subida (`ExportRecord.owned=True`) -> `shutil.rmtree` del directorio de trabajo ENTERO (es nuestro, vive bajo `CEM_WORK_DIR`); modo local (`owned=False`) -> la carpeta es del USUARIO, se borra SOLO `_output/` (lo unico que `POST /convert` creo ahi), la carpeta original nunca se toca. En los dos modos se olvida el registro (`ExportStore.remove`, metodo nuevo) y cualquier job asociado (`JobStore.remove_by_export`, metodo nuevo). `export_id` inexistente -> 404 (decision propia, spec 06 no lo aclara: se elige NO ser idempotente, por consistencia con el resto del contrato - un cliente que quiera un DELETE "a ciegas" puede tratar el 404 como exito igual). Tests: apps/api/tests/test_output.py (18: arbol coherente con lo que `convert()` escribio, determinismo entre llamadas, 404 distinto para "nunca convertido" vs "export inexistente", contenido de archivo byte a byte contra el disco, archivo dentro de subcarpeta con `/`, 5 variantes de path traversal incluidas barra invertida/ruta absoluta/`C:\`/`..` intercalado -> 400 sin filtrar contenido, `path` ausente -> 400/422, archivo inexistente -> 404, zip descargado abre con `zipfile.ZipFile` y contiene los archivos esperados) y test_delete_export.py (7: subida borra el directorio completo y el id deja de resolver, local conserva la carpeta del usuario con su contenido original y solo borra `_output/`, local nunca convertido no toca nada, jobs asociados se olvidan, id inexistente -> 404, segunda llamada sobre el mismo id ya borrado -> 404). 648 tests en verde (core+cli+api, +23 de esta ronda), cobertura de core sin cambios (99.40%, no se toco packages/core).
- [x] Docker Compose (api :8000) + openapi.json valido (spec 06 CA-7, CA-9). `apps/api/Dockerfile` (multi-stage: builder con `ghcr.io/astral-sh/uv:python3.12-bookworm-slim` corriendo `uv sync --frozen --no-dev --package claude-export-md-api` sobre el build context completo del repo -necesario porque apps/api depende de packages/core via workspace de uv-, runtime en `python:3.12-slim-bookworm` copiando solo `/app` ya resuelto), `docker-compose.yml` en la raiz (solo servicio `api`, puerto 8000, `CEM_ALLOW_LOCAL_PATHS=false`/`CEM_MAX_UPLOAD_MB=2048`/`CEM_JOB_TIMEOUT_S=300` explicitos) y `.dockerignore` nuevo (excluye exports/, output/, .venv/, node_modules/, etc. del build context). NO hay servicio `web` todavia: apps/web sigue siendo el placeholder de M0 (scripts/noop-*.cjs), construir una imagen para una app Angular que no existe no tiene sentido - se agrega en M4 cuando el front sea real. Verificado con Docker real (no solo `docker compose config`): `docker compose build api` + `docker compose up -d api` levanta el contenedor de verdad; `curl localhost:8000/health` -> `{"status":"ok"}`; `curl localhost:8000/openapi.json` -> 200, JSON valido con los 10 paths esperados (los 9 de spec 06 + `/health`); `POST /api/v1/exports {"path":...}` sin la variable en `true` -> 403 problem+json real (CA-9 confirmado end-to-end, no solo por test). `just gen-client` sigue sin correrse (ya estaba anotado desde M0): apps/web no tiene `openapi-typescript-codegen` como dependencia todavia, se instala en M4 junto con el resto del front. README.md reescrito con instrucciones de despliegue para las 3 vias (CLI, API+Docker con ejemplos de curl del flujo completo, web pendiente) y una seccion de desarrollo (`just setup/test/lint/api/up`).
- [x] **Ronda de fixes de la revision de architect-reviewer (RECHAZO de M3, primero del proyecto): los 4 bloqueantes + las 6 observaciones, TDD (test que reproduce -> fix) en todos los casos, sin tocar `packages/core`.**
  1. **Bloqueante 1 (subida multipart perdia archivos en silencio, el mas grave)** - `services/exports.py::register_upload` iteraba `form.values()` sobre el `FormData` de Starlette, que DEDUPLICA por nombre de campo: subir 3 partes con `name="file"` (exactamente el ejemplo de curl del README) dejaba solo 1 archivo en disco con un 201, sin ningun aviso. Cambiado a `form.multi_items()` (itera TODAS las partes sin importar si comparten nombre), tanto para armar la lista de `uploads` como en el `finally` que cierra los `UploadFile` (antes solo cerraba los que sobrevivian la deduplicacion, dejando `SpooledTemporaryFile` huerfanos de los descartados). Test de regresion reescrito para asertar el EFECTO real (`test_exports_upload.py::test_multiple_files_can_be_uploaded_together`, ahora con 3 partes `name="file"` y `sorted(p.name for p in work_dir.iterdir())`) - el test anterior solo miraba `status_code == 201`, que es justo lo que enmascaraba el bug (confirmado: con el codigo viejo el test reescrito fallaba mostrando 1 archivo en vez de 3).
  2. **Bloqueante 2 (README con el ejemplo roto)** - resuelto TAL CUAL con el fix del bloqueante 1 (el ejemplo de curl del README ya usaba `name="file"` repetido tres veces, que es exactamente el caso que perdia archivos): no hizo falta cambiar el README. Verificado con la API REAL corriendo (`uv run uvicorn`, no solo el test): las 3 partes (`conversations-000.zip`, `memories-000.zip`, `member-manifest-*.json`) subidas con `curl -F "file=@..."` tres veces quedan las 3 en disco y `/inventory` reconoce ambas categorias (`conversations` y `memories`) mas el manifiesto. Vuelto a confirmar contra el CONTENEDOR DOCKER real (`docker compose build api` + `up`, ver punto 5) con el mismo resultado.
  3. **Bloqueante 3 (descarga de zip bloqueaba el event loop 5s al abortar)** - `services/output.py::stream_zip`: el `finally` del generador async hacia `thread.join(timeout=5.0)` SINCRONO; al abortar una descarga (`GeneratorExit` en el `yield`), eso bloqueaba el hilo del event loop hasta 5s (medido por el reviewer: `/health` tardaba 5.011s despues), y el hilo productor (`_write_zip_to_queue`) quedaba vivo para siempre bloqueado en `queue.put()` sobre una cola que nadie mas iba a drenar. Fix en 3 partes: (a) `_QueueWriterStream.write` ahora recibe un `threading.Event` de cancelacion y hace `put(chunk, timeout=0.1)` en un loop que revisa el evento en cada vuelta, en vez de un `put()` bloqueante sin limite - asi el hilo productor nota la cancelacion en <=100ms incluso si estaba esperando espacio en una cola llena; (b) el `finally` de `stream_zip` activa el evento, DRENA la cola (`get_nowait()` hasta `queue.Empty`) para liberar cualquier `put()` en curso, y recien despues espera al hilo; (c) ese join ahora es `await asyncio.to_thread(thread.join, 5.0)`, nunca sincrono sobre el hilo del event loop. Tests (`test_output.py`, sin necesitar un cliente HTTP real que corte la conexion, solo `gen.__anext__()` + `gen.aclose()` para simular el `GeneratorExit`): `test_stream_zip_aborted_midway_does_not_block_the_event_loop` (con codigo viejo: 5.015s medidos; con el fix: <1s) y `test_stream_zip_aborted_midway_does_not_leave_the_writer_thread_running_forever` (`threading.enumerate()` por nombre, el hilo productor ahora se nombra `cem-zip-writer`). El root de prueba usa 20MB de datos incompresibles (`random.randbytes`) para forzar que el productor necesite bloquearse en `put()` de verdad antes de terminar (backpressure real con `maxsize=8`).
  4. **Bloqueante 4 (byte NUL en `path=` rompia el contrato con un 500 crudo)** - `services/output.py::_validate_relative_path` no rechazaba caracteres de control (NUL incluido) sobre el string crudo: `candidate.resolve()` mas abajo lanzaba `ValueError: embedded null character in path`, que NINGUN exception handler atrapaba -> 500 con `content-type: text/plain` (confirmado reproduciendo el traceback completo antes del fix: `pathlib.py::stat` -> `os.stat` -> `ValueError`). Fix en dos partes: (a) `_validate_relative_path` ahora rechaza `any(ord(ch) < 0x20 for ch in raw)` ANTES de construir ningun `Path`, con el mismo 400 problem+json que ya usaba para `..`; (b) `main.py` gano un `@app.exception_handler(Exception)` generico (red de seguridad para CUALQUIER excepcion no anticipada en cualquier endpoint futuro, no solo esta) que traduce a `500 application/problem+json` con un `detail` GENERICO ("Error interno", nunca el mensaje real ni el stack trace) y loguea el detalle real con `logger.exception`. Tests: `test_output.py::test_a_path_with_a_control_character_is_rejected_with_400_never_500` (parametrizado con NUL, `\x01` y `\n` contra un export YA convertido, confirmado que el caso NUL sí crasheaba con el codigo viejo mostrando el traceback completo de `pathlib`) y `test_error_handling.py` (nuevo archivo): `test_an_arbitrary_unhandled_exception_becomes_a_500_problem_json_not_a_raw_500` (monkeypatch de `exports_service.inventory_for` para forzar un `RuntimeError` sin ninguna relacion con path traversal, confirmado que SIN el handler generico el `content-type` es `text/plain` y CON el handler es `application/problem+json`). Confirmado ademas contra el CONTENEDOR DOCKER real: `path=_index.md%00.txt` contra un export convertido de verdad -> `400 application/problem+json` (`"'_index.md\x00.txt' contiene un caracter de control invalido."`).
  5. **Observacion 1 (2 jobs de conversion concurrentes sobre el mismo export)** - `services/jobs.py::JobStore` gano `find_active_job_id(export_id)` (usado tambien por la observacion 2) y `create_if_no_active_job(job_id, export_id)`, ATOMICO bajo un unico `with self._lock` (separar "chequear" de "crear" en dos llamadas dejaria una ventana TOCTOU entre 2 `POST /convert` concurrentes). `start_conversion` ahora devuelve `409` problem+json con el `job_id` existente en el `detail` si ya hay un job `pending`/`running` para ese export; un `POST /convert` posterior vuelve a aceptarse normalmente en cuanto ese job llega a `done`/`failed`. Tests en `test_convert_jobs.py`: `test_a_second_convert_while_one_is_still_running_is_409_with_the_existing_job_id` (con `run_convert` deliberadamente lento via monkeypatch, igual que el test de CA-2 existente; confirmado que con el codigo viejo el segundo POST daba 202) y `test_convert_is_allowed_again_once_the_previous_job_reached_a_terminal_status`.
  6. **Observacion 2 (`DELETE` durante un job en curso recreaba `_output/` despues del borrado)** - `services/lifecycle.py::delete_export` ahora usa `ExportStore.get` (no destructivo) en vez de `remove` directo, para poder chequear `jobs_service.get_job_store().find_active_job_id(export_id)` ANTES de decidir si borrar; si hay un job `pending`/`running`, `409` problem+json con el `job_id` en el `detail`, y el registro NO se toca. Test en `test_delete_export.py::test_delete_while_a_conversion_is_in_progress_is_rejected_with_409`: reproducido primero con el codigo viejo (204 real, y el log de la conversion en curso mostraba un `Permission denied` al intentar leer `export.zip` porque el `DELETE` ya habia borrado el directorio de trabajo entero mientras el job seguia leyendolo - exactamente el sintoma que describio el reviewer) y `test_delete_is_allowed_again_once_the_job_reached_a_terminal_status`.
  7. **Observacion 3 (`destination`, ruta absoluta del servidor, se filtraba en el evento SSE `done`)** - `GET /jobs/{id}` (`JobStatusResponse`) deliberadamente no incluye `destination`, pero `stream_job_events` si lo mandaba en el evento `done` - inconsistente y una fuga de la estructura de directorios interna. Sacado del payload SSE; test de regresion `test_convert_jobs.py::test_events_final_done_payload_never_leaks_the_server_side_destination_path`.
  8. **Observacion 4 (`.dockerignore` sin `**/`)** - los patrones (`*.zip`, `member-manifest-*.json`, `.venv/`, `__pycache__/`, `.mypy_cache/`, `.ruff_cache/`, `.pytest_cache/`, `.coverage`, `htmlcov/`, `node_modules/`) solo matcheaban en la RAIZ del build context (confirmado por el reviewer: `__pycache__` de `packages/core/src/**` entraba en la imagen real). Todos esos patrones ahora llevan el prefijo `**/`; `exports/` y `output/` quedan TAL CUAL en la raiz (son las unicas carpetas donde CLAUDE.md 3 dice que pueden existir, via el uso local del CLI).
  9. **Observacion 5 (Dockerfile como root, sin healthcheck)** - `apps/api/Dockerfile`: `RUN useradd -m app && chown -R app /app` + `USER app` antes del `CMD` (confirmado con el contenedor real: `docker compose exec api whoami` -> `app`, no `root`). `docker-compose.yml`: `healthcheck` en el servicio `api` contra `/health` via `python -c "import urllib.request; ..."` (sin `curl` disponible en `python:3.12-slim-bookworm`, la stdlib de Python si esta siempre) - confirmado con `docker compose ps` mostrando `Up ... (healthy)` tras el `start_period`.
  10. **Observacion 6 (`docs/specs/06-api.md` desactualizado)** - agregada una seccion "Detalles de contrato" con lo que faltaba: `only` sigue siendo no-op (documentado, no implementado - CLAUDE.md 11 pide no implementarlo todavia), el 404 de "export sin convertir" vs `export_id` inexistente, el 403 de CA-9, el 413 de subida (con la limitacion conocida de Starlette ya anotada en STATUS.md), el matiz local-vs-subida de `DELETE` (ahora con el 409 de la observacion 2), los campos reales de `GET /jobs/{id}` (incluidos `warnings`/`reason`, y la ausencia deliberada de `destination`), y la aclaracion de `multi_items()` en la subida (bloqueante 1). La tabla de endpoints tambien se actualizo con los nuevos codigos (`409` en `/convert` y `DELETE`, `404`/`400` en `/output/*`).
  - **Resultado**: 659 tests en verde (core+cli+api; +11 sobre los 648 de la linea anterior: 1 subida reescrita para asertar el efecto real, 2 de `stream_zip` abortado, 3 de byte de control (parametrizado) + 1 de `test_error_handling.py` nuevo, 2 de jobs concurrentes + 1 de fuga de `destination`, 2 de `DELETE` durante conversion), cobertura de `core` sin cambios (99.45%, no se toco `packages/core`). `ruff check`/`ruff format --check` en verde sobre todo el repo. Docker real reconstruido y confirmado end-to-end para los bloqueantes 1 y 4 (ver arriba) ademas del healthcheck/usuario no-root de las observaciones 5.
- [x] **Segunda revision de architect-reviewer (2do RECHAZO de M3)**: confirmo, contra un contenedor Docker real, que 8 de los 10 problemas de la ronda anterior quedaron genuinamente resueltos. Encontro 2 bugs reales sobrevivientes, ambos de la MISMA familia (un hilo en segundo plano sobrevive a lo que se supone que lo controla): (1) el hilo `cem-zip-writer` de `services/output.py` puede quedar bloqueado para siempre en `queue.put()` ante un abort DURO (RST/`SO_LINGER(1,0)`) porque `cancel_event` solo se activa desde el `finally` de `stream_zip`, que solo corre si el generador se cierra de forma PROLIJA (`aclose()`) - reproducido contra un uvicorn real en Windows; y (2) el hilo real de `asyncio.to_thread(run_sync)` de un job vencido por `CEM_JOB_TIMEOUT_S` sigue escribiendo en `_output/` despues de que el job se marca `failed`/`timeout`, pero los guards de la ronda anterior (`find_active_job_id`/`create_if_no_active_job`) solo miraban el ESTADO del job (`pending`/`running`), asi que ya no lo consideraban activo y dejaban pasar tanto un nuevo `POST /convert` como un `DELETE` que competian con el hilo vencido.
- [x] **Ronda 3 de fixes (los 2 bugs de la segunda revision), TDD en los dos, sin tocar `packages/core` ni `apps/cli`.**
  1. **Bug 1 (hilo `cem-zip-writer` con vida acotada por construccion, en vez de depender de que algo externo lo cancele)** - `services/output.py` gana `_WRITER_MAX_IDLE_S` (30.0s) y `_QueueWriterStream` ahora lleva `self._last_progress` (actualizado en CADA `put()` que tiene exito). El loop de `write()` sigue reintentando `put(chunk, timeout=_WRITE_POLL_INTERVAL_S)` (0.1s, sin cambios), pero si pasan mas de `_WRITER_MAX_IDLE_S` desde el ULTIMO `put()` exitoso (nadie esta consumiendo la cola), corta con `_DownloadCancelled` DESDE ADENTRO del hilo - reusando la misma excepcion interna que ya usaba el cierre prolijo (`_write_zip_to_queue` ya la atrapaba en su `except _DownloadCancelled: pass`, no hizo falta tocar ese metodo). El cierre PROLIJO sigue siendo mucho mas rapido (nota la cancelacion en <=100ms via `cancel_event`); el timeout de inactividad es SOLO la red de seguridad para cuando nadie avisa. Ademas, `services/lifecycle.py::delete_export` cambia sus dos `shutil.rmtree(path, ignore_errors=True)` por `_rmtree_best_effort(path)` (funcion nueva), que usa `shutil.rmtree(path, onexc=_log_rmtree_failure)` (soportado desde Python 3.12, la version que ya pedia `pyproject.toml`) - mismo contrato de nunca lanzar, pero cada archivo/carpeta que no se pudo borrar ahora queda anotado con `logger.warning` en vez de desaparecer en silencio; el `DELETE` sigue respondiendo `204` en cualquier caso (no se toco el contrato ya publicado). Tests: `apps/api/tests/test_output.py::test_writer_thread_terminates_on_its_own_when_nobody_drains_the_queue` (deterministico, SIN sockets ni timing de red real: una `queue.Queue(maxsize=1)` ya llena que la prueba nunca vacia, `_WRITER_MAX_IDLE_S` bajado a 0.2s via monkeypatch, y se confirma que el hilo productor termina solo en <3s SIN que la prueba llame nunca a `cancel_event.set()` - reproducido primero en rojo: con el codigo viejo `monkeypatch.setattr(output_service, "_WRITER_MAX_IDLE_S", ...)` fallaba porque la constante ni existia) y `apps/api/tests/test_delete_export.py` (+2: `test_rmtree_best_effort_logs_a_warning_for_each_failure_instead_of_swallowing_it`, con un doble de `shutil.rmtree` que invoca `onexc` con un fallo sintetico para probar el cableado sin depender de locking de archivos real; y `test_delete_of_an_uploaded_export_logs_when_a_locked_file_survives_the_rmtree`, confirmacion end-to-end con un archivo REALMENTE abierto sin compartir borrado - el default de `open()` en Windows, el mismo SO de la reproduccion del reviewer -, `skipif` fuera de Windows porque el locking de archivos no es portable). **No se probo contra un servidor real con un abort duro (RST) por Docker/sockets**: dado el tiempo disponible se prioriza el test unitario/determinista que fuerza la condicion exacta (cola llena, nadie la vacia) en vez de orquestar un cliente que corte la conexion a mitad de una descarga real - el mecanismo del fix es independiente de la capa de transporte (el hilo no sabe ni le importa si el consumidor se fue por un `aclose()` prolijo o por un RST), asi que el test determinista cubre la misma rama de codigo que un abort real ejercitaria.
  2. **Bug 2 (un job "activo" pasa a significar "el worker realmente sigue vivo", no el ESTADO del job)** - `services/jobs.py::_JobState` gana `finished_event: threading.Event` (`default_factory`, uno por job). `_run_job::run_sync` (el callable que corre DENTRO de `asyncio.to_thread`) ahora tiene su propio `try/finally` que llama a `finished_event.set()` en el `finally` - se activa cuando el hilo REAL termina de verdad (exito, excepcion, o `convert()` completando tarde despues de que `asyncio.wait_for` ya desistio por timeout), nunca cuando el orquestador async decide dejar de esperarlo. `JobStore` gana `_is_active(job)` (metodo estatico compartido) y `get_finished_event(job_id)` (deliberadamente NO expuesto en `JobView`/`GET /jobs/{id}`, es un detalle interno de sincronizacion); `find_active_job_id`/`create_if_no_active_job` reemplazan su chequeo `job.status in _ACTIVE_JOB_STATUSES` por `self._is_active(job)`, que agrega: un job `failed` con `reason == TIMEOUT_REASON` sigue contando como activo MIENTRAS `finished_event` no este seteado. Sigue siendo atomico bajo el mismo `with self._lock` que ya tenian las dos funciones (ninguna ventana de carrera nueva: leer `job.status`/`job.reason`/`job.finished_event.is_set()` pasa a ser parte de la MISMA seccion critica que ya existia, y `threading.Event.is_set()` es thread-safe por si solo). Tests en `apps/api/tests/test_convert_jobs.py` (+4): 3 unitarios y deterministicos directo sobre `JobStore` (sin threads ni timing real) - `test_job_store_a_timed_out_job_is_still_active_until_its_finished_event_fires` (el caso central: `mark_failed(..., TIMEOUT_REASON)` sin setear el evento sigue siendo activo; `finished_event.set()` lo libera), `test_job_store_a_job_failed_for_a_reason_other_than_timeout_is_never_active` (un `failed` normal nunca bloquea, tenga o no evento seteado) y `test_job_store_a_pending_or_running_job_is_active_regardless_of_finished_event` (el caso ya cubierto por la ronda anterior no se rompe) - mas uno end-to-end, `test_timed_out_job_still_blocks_convert_and_delete_until_the_real_thread_finishes`, con `CEM_JOB_TIMEOUT_S=0.05` y `run_convert` monkeypatcheado para tardar 0.6s: justo despues del timeout (`status == "failed"`, `reason == "timeout"`, `finished_event` todavia sin setear) tanto un nuevo `POST /convert` como `DELETE` dan `409`, y solo dejan de darlo despues de `finished_event.wait(timeout=10.0)` (nunca un sleep arbitrario) - reproducido primero en rojo: con el codigo viejo `find_active_job_id` devolvia `None` para el job `failed`/timeout y el test end-to-end fallaba con `AttributeError: 'JobStore' object has no attribute 'get_finished_event'`.
  - **Resultado**: 666 tests en verde (core+cli+api; +7 sobre los 659 de la linea anterior: 1 del hilo con vida acotada, 2 de `_rmtree_best_effort`, 4 de `finished_event`/jobs vencidos), cobertura de `core` sin cambios (99.45%, no se toco `packages/core`). `ruff check --fix`/`ruff format` en verde sobre `apps/api`. **No se repitio la confirmacion contra un contenedor Docker real en esta ronda** (ver nota del bug 1 arriba): los dos bugs se reprodujeron y confirmaron con tests unitarios/de integracion deterministicos, sin sockets ni timing de red real, priorizando velocidad y estabilidad en CI sobre una repeticion del escenario de abort duro contra un servidor real.
- [x] **Tercera revision de architect-reviewer (3er RECHAZO de M3, mismo endpoint las tres veces): `GET /exports/{id}/download`.** A pesar de dos rondas de parches (bloqueante 3 + bug 1 de la ronda 3, ambos arriba), el mecanismo de hilo-productor + `queue.Queue` acotada seguia teniendo un bug de fondo: si el consumidor dejaba de leer SIN cerrar la conexion de forma prolija, el hilo productor podia morir sin lograr entregar el centinela de "termine" y el lado que lee quedaba esperando para siempre en el mismo pool de hilos de `asyncio.to_thread` que comparten las conversiones - con unas pocas descargas asi, la API completa quedaba trabada hasta reiniciar el proceso. Ademas se midio throughput real de ~42 KB/s por el diseno de ir al pool de hilos por cada chunk. **Decision del usuario (explicita, tras ver el historial de 3 rechazos sobre la MISMA pieza): dejar de parchear y SIMPLIFICAR** - abandonar el streaming incremental por completo en vez de intentar un cuarto parche sobre el mismo mecanismo.
- [x] **Ronda 4 de fixes: reemplazo completo del mecanismo de `/download` (no un parche mas), sin tocar `packages/core` ni `apps/cli`.** **NOTA: la afirmacion de esta linea de que "la ejecucion SIEMPRE llega a `await self.background()`" es FALSA - corregido en la ronda 5, ver mas abajo (FileResponse.__call__ no corre background si el header Range es invalido/no-satisfacible; el diseno final usa TempZipResponse con un finally propio, no BackgroundTask).** Las 3 rondas anteriores de este bug (bloqueante 3, bug 1 de ronda 3, y este RECHAZO) quedan resueltas por REEMPLAZO del mecanismo que las causaba a las tres, no por otro parche encima: `_QueueWriterStream`, `_write_zip_to_queue`, `stream_zip`, `cancel_event`, `_WRITER_MAX_IDLE_S`, `_DownloadCancelled`, `_ZIP_CHUNK_QUEUE_MAXSIZE` y `_WRITE_POLL_INTERVAL_S` se ELIMINARON enteros de `services/output.py` (junto con los imports `queue`/`threading`/`time`/`AsyncIterator` que quedaban huerfanos ahi - `services/jobs.py` sigue usando su propio `threading.Event` para `finished_event`, sin relacion con esto, no se toco). **Diseno nuevo**: `services/output.py::build_zip_archive(output_dir: Path) -> Path` arma el zip COMPLETO en un archivo temporal (`tempfile.mkstemp(prefix="cem-download-", suffix=".zip")`, `os.close(fd)` antes de reabrir con `zipfile.ZipFile` en modo escritura normal - el temporal SI es seekable, asi que ya no hace falta el truco de "data descriptors" del stream no-seekable de antes) recorriendo `output_dir` con el mismo `_iter_files_sorted` (orden deterministico) que ya usaba el mecanismo viejo; si algo falla a mitad de camino, borra el temporal parcial (`zip_path.unlink(missing_ok=True)`) antes de relanzar. `services/output.py::remove_temp_zip(zip_path: Path) -> None` es el `background` de la respuesta. `routers/output.py::download_export` sigue llamando `require_output_dir` PRIMERO (mismo criterio de siempre: 404 antes de construir nada), arma el zip con `zip_path = await asyncio.to_thread(build_zip_archive, root)` (UN solo `asyncio.to_thread` para toda la operacion, nunca uno por chunk) y devuelve `FileResponse(zip_path, media_type="application/zip", filename=..., background=BackgroundTask(remove_temp_zip, zip_path))`. **Confirmacion de que el temporal siempre se borra, incluso si el cliente corta a mitad de camino**: no fue practico simularlo con `TestClient` (su transporte ASGI corre la app COMPLETA en un portal sincrono antes de devolver el control al test, sin importar si el test lee el body despues o no - confirmado leyendo `starlette.testclient._TestClientTransport.handle_request`), asi que se confirmo LEYENDO el codigo real de produccion: `starlette.responses.FileResponse.__call__` llama `await self.background()` DESPUES del loop de envio, sin ningun try/except que lo salteara; y `uvicorn.protocols.http.{h11,httptools}_impl.py::send()` empieza con `if self.disconnected: return` - un cliente desconectado hace que cada `send()` sea un no-op silencioso en vez de una excepcion, asi que el loop de envio SIEMPRE termina normalmente y la ejecucion SIEMPRE llega a `await self.background()`. Es la misma garantia documentada de Starlette (`BackgroundTask` de una `Response` corre pase lo que pase), verificada linea por linea contra la version instalada en vez de asumida. **Tests** (`apps/api/tests/test_output.py`): se BORRARON los 3 que probaban el mecanismo interno que ya no existe (`test_stream_zip_aborted_midway_does_not_block_the_event_loop`, `test_stream_zip_aborted_midway_does_not_leave_the_writer_thread_running_forever`, `test_writer_thread_terminates_on_its_own_when_nobody_drains_the_queue`, con su helper `_make_incompressible_root`); los 3 tests de comportamiento observable de `/download` que YA EXISTIAN (zip valido con todos los archivos esperados, 404 de export sin convertir, 404 de export inexistente) se dejaron TAL CUAL (siguen siendo validos contra el diseno nuevo, no probaban el mecanismo interno). Se AGREGARON 3 tests nuevos contra el diseno simple: `test_download_does_not_leave_a_temp_zip_behind` (compara el contenido del directorio temporal del sistema, filtrado por el patron `cem-download-*.zip`, antes y despues de una descarga completa), `test_two_downloads_in_a_row_do_not_clobber_each_other` (dos descargas seguidas sobre el mismo export, ambas validas, contenido identico, ningun temporal huerfano - cada una arma el suyo propio via `tempfile.mkstemp`, que da nombres unicos por construccion) y `test_a_failure_while_building_the_zip_cleans_up_the_partial_temp_file` (`monkeypatch` de `_iter_files_sorted` para forzar una excepcion a mitad de camino: confirma 500 problem+json - via el exception handler generico de `main.py`, sin cambios - y que el temporal parcial NO queda huerfano). `services/lifecycle.py` (el `DELETE`) y `test_delete_export.py` se REVISARON sin encontrar acoplamiento al mecanismo viejo (solo prosa historica en comentarios sobre el bug ya resuelto, ningun import ni llamada); no se modificaron. `docs/specs/06-api.md` (tabla de endpoints + nueva vinieta en "Detalles de contrato") y este archivo se actualizaron para describir el diseno nuevo. **Resultado**: 666 tests en verde (core+cli+api; se borraron 3 del mecanismo viejo y se agregaron 3 del diseno nuevo, neto sin cambio sobre los 666 de la ronda anterior), cobertura de `core` sin cambios (99.45%, no se toco `packages/core`). `ruff check --fix`/`ruff format` en verde sobre `apps/api`.
- [x] **Ronda 5 de fixes (4to rechazo, el ultimo): `FileResponse.__call__` no corre `background` para un `Range` invalido/no-satisfacible.** El revisor de la ronda 4 confirmo que el reemplazo del mecanismo de streaming resolvio de raiz las rondas 1-3, pero encontro que `starlette.responses.FileResponse.__call__` hace `return` temprano (400 `MalformedRangeHeader`, 416 `RangeNotSatisfiable`) ANTES de la linea `await self.background()` - como cada `/download` arma un zip nuevo, un header `Range` calculado contra una descarga anterior puede caer fuera de tamano, y el temporal quedaba huerfano para siempre (reproducido: 1 copia completa del export comprimido fugada por request). Fix (aplicado directo por el orquestador, sin otra ronda de agente, dado lo puntual): `services/output.py::TempZipResponse(FileResponse)` sobreescribe `__call__` con un `try/finally` propio que llama `remove_temp_zip` SIEMPRE, sin depender de si `super().__call__` llego a la linea de `background` o no; `routers/output.py::download_export` devuelve `TempZipResponse(...)` en vez de `FileResponse(..., background=BackgroundTask(...))`. `remove_temp_zip` sigue existiendo (ya no es un `BackgroundTask`, es una llamada directa desde el `finally`). Docstrings de `remove_temp_zip`/`TempZipResponse` y el comentario del router corregidos para no repetir la afirmacion falsa de "el background corre siempre". Test nuevo parametrizado `test_a_range_request_does_not_leave_a_temp_zip_behind` (`apps/api/tests/test_output.py`): `Range` malformado -> 400 sin huerfanos, `Range` no satisfacible -> 416 sin huerfanos, `Range` valido -> 206 de control (siempre paso por `background`, no es el caso que fallaba). El comentario largo que documentaba "por que no se puede simular el corte con TestClient" se corrigio para reflejar que la garantia de Starlette es real para el camino normal pero no cubre los dos `return` de `Range` (antes decia que cubria "siempre"). 669 tests en verde (666 + 3 del test parametrizado), cobertura de `core` sin cambios (99.45%). Con esto, M3 queda CERRADO tras 5 rondas de revision sobre `/download` (4 rechazos + este ultimo fix); el resto de la API se aprobo integra en la ronda 2 y no volvio a tocarse.

## M4 - Web (EN CURSO)
- [x] **Scaffold real de apps/web** (Angular 21.2.23 standalone/signals, Angular Material 21.2.14, Tailwind 3.4.19, Transloco 8.4.0, ESLint con angular-eslint 21.4.0) + cliente OpenAPI generado (`just gen-client`, corregido) + layout/routing base con lazy loading. Reemplaza el placeholder de M0 (`package.json` con scripts `noop-*.cjs`, ahora borrados junto con `scripts/`).
  **Comandos exactos usados** (Node 20.19.0, pnpm 9.15.9, todo dentro de `apps/web` salvo donde se indica): `npx @angular/cli@21 new claude-export-md-web --directory . --routing --style=scss --skip-git --package-manager=pnpm --force --skip-install` (con `--directory .` porque `ng new .` no acepta `.` como nombre de proyecto - falla el schema de nombres; `--force` para sobrescribir `pnpm-lock.yaml` del placeholder) → `pnpm install` → `npx ng add @angular/material --defaults --skip-confirmation` → `pnpm add -D tailwindcss@3 postcss autoprefixer` + `npx tailwindcss init` → `pnpm add @jsverse/transloco` → `pnpm add -D openapi-typescript-codegen` → `npx ng add angular-eslint@21 --skip-confirmation` (el primer intento con `@angular-eslint/schematics` sin pin de version instalo la v22, incompatible con este workspace v21, y el propio schematic lo detecto y aborto solo con instrucciones - hubo que reintentar con `angular-eslint@21` explicito) → `pnpm add @angular/animations@^21.2.0` (ver mas abajo, necesaria para `provideAnimationsAsync()`, `ng add material` no la agrega sola).
  **Decisiones/ajustes sobre el enfoque sugerido, con motivo:**
  1. **Angular 21 genero la app SIN zone.js** (no aparece en `package.json`, no hay `provideZoneChangeDetection`/`provideExperimentalZonelessChangeDetection` en `app.config.ts`): el scheduler de Angular 21 ya no depende de Zone.js por defecto para change detection basada en signals/router/async pipe; no hizo falta agregar nada a mano, los tests (`TestBed` + `fixture.detectChanges()`) funcionan igual.
  2. **Convencion de nombres de archivo "2025"** (default nuevo del schematic, visible en `app.ts`/`app.routes.ts` en vez de `app.component.ts`): se siguio para TODOS los archivos nuevos (`upload-page.ts`, no `upload-page.component.ts` como sugiere .claude/skills/angular-feature/SKILL.md, escrito antes de este default) - es lo que generaria `ng generate component` en este workspace tal cual quedo configurado; se documenta aca para que la proxima ronda no se sorprenda con la discrepancia con el skill.
  3. **Test runner: Vitest, no Karma/Jasmine** (`@angular/build:unit-test`, default de `ng new` en Angular 21). `pnpm test -- --watch=false` sigue funcionando igual que con Karma (el flag existe en el builder nuevo).
  4. **Angular Material 21 usa el nuevo `mat.theme()` con variables CSS** (Material 3 nativo, no el `mat.core()` + `mat.all-component-themes()` de temas prebuilt de versiones anteriores) - es lo que genera `ng add @angular/material --defaults` en esta version, no hizo falta elegir nada a mano; paleta azure/blue elegida por el schematic (queda como base neutra, no es una decision de diseño de esta ronda).
  5. **Se quitaron los `<link>` a fonts.googleapis.com que agrega `ng add @angular/material` por defecto** (Roboto + Material Icons) en `src/index.html`: CLAUDE.md §5/§1.4 - "nada sale de la maquina del usuario, no hay llamadas a Internet" - una app pensada para correr 100% local (Docker Compose sin salida a Internet) no deberia depender de una CDN externa solo para tipografia. Documentado con un comentario en el propio `index.html`; queda pendiente para cuando se agregue `mat-icon` real usar SVGs locales en vez de la fuente de ligaduras de Google.
  6. **Tailwind 3 integrado via el mecanismo estandar documentado por Angular** (https://angular.dev/guide/styling/tailwind): `tailwind.config.js` en la raiz del proyecto (con `content: ['./src/**/*.{html,ts}']`) + directivas `@tailwind base/components/utilities` en `src/styles.scss` - el Angular CLI (`@angular/build:application`, el builder esbuild nuevo, no solo el viejo webpack) detecta el archivo y aplica el pipeline de PostCSS solo, SIN `postcss.config.js` aparte. Un detalle real de Sass que no estaba anticipado: `@use` tiene que ir ANTES que cualquier otra regla en un archivo `.scss` (incluidas las `@tailwind`), asi que las directivas de Tailwind quedan DESPUES del `@use '@angular/material' as mat;` que agrego `ng add material`, no antes - poniendolas antes rompe el build (`@use rules must be written before any other rules`, confirmado en rojo antes del fix).
  7. **`public/assets/i18n/{es,en}.json`, no `src/assets/i18n/`**: Angular 17+ reemplazo la carpeta `src/assets/` por `public/` como raiz de estaticos servidos tal cual (`angular.json` -> `assets: [{glob: '**/*', input: 'public'}]`); `src/assets/` ya NO se copia al build en este scaffold, asi que los JSON de Transloco viven en `public/assets/i18n/` para que la URL sea la misma (`assets/i18n/es.json`) que pedia CLAUDE.md §7 sin depender de una carpeta que Angular 21 no incluye por defecto.
  8. **`@angular/animations` como dependencia directa, con nota**: `pnpm add @angular/animations` mostro `WARN deprecated @angular/animations@21.2.23: ... Use animate.enter/animate.leave` - esa deprecacion es sobre la API vieja de definir animaciones PROPIAS con `trigger()/state()/transition()` en `@Component({ animations: [...] })`; Angular Material sigue necesitando el paquete instalado igual (sus componentes internos - overlay, ripple, snackbar - importan simbolos de `@angular/animations` en su codigo compilado, con o sin Noop), asi que no es opcional para usar Material. Se eligio `provideAnimationsAsync()` (carga el motor de animaciones en un chunk aparte, no en el bundle inicial) en vez de `provideAnimations()`/`provideNoopAnimations()`, para no sumarle peso al bundle inicial (CA-6) sin perder las transiciones reales de Material.
  9. **`justfile::gen-client` corregido, no solo usado**: el comando "de libro" tenia dos problemas reales, no hipoteticos (reproducidos en rojo antes del fix): (a) el binario que instala el paquete `openapi-typescript-codegen` se llama `openapi` (ver su `package.json`, campo `bin`), no el nombre del paquete - `pnpm exec openapi-typescript-codegen` falla con "no se reconoce como un comando"; (b) la version de `json-schema-ref-parser` que trae `openapi-typescript-codegen@0.31.0` ya no resuelve una URL http(s) pasada directo como `--input` (falla con `SyntaxError: Unable to resolve $ref pointer "http://localhost:8000/openapi.json"` intentando LEERLA COMO ARCHIVO, confirmado que la URL respondia bien con `curl`/`fetch` nativo de Node en paralelo). Fix: `just gen-client` ahora baja el `openapi.json` a un archivo temporal con `node -e "fetch(...).then(...)"` (Node ya es un prerequisito de pnpm, no se agrega nada nuevo) y le apunta `--input` a ESE archivo, borrandolo al final.
  10. **`features/settings` agregado** (no estaba en la lista de CLAUDE.md §3 - upload/inventory/jobs/browser -, pero spec 07 tiene 5 pantallas y la 5ta es Ajustes): se creo siguiendo la misma receta que las otras 4, con un comentario en el placeholder explicando la discrepancia con CLAUDE.md §3 para que se revise si hace falta actualizarlo.
  11. **`proxy.conf.json` + `angular.json::serve.options.proxyConfig`** (no pedido explicitamente en la consigna, agregado como parte de "routing/layout base" para que `pnpm start` funcione contra la API real en dev sin CORS): reenvia `/api` y `/health` a `http://localhost:8000`. `OpenAPI.BASE` del cliente generado queda en `''` (default), pensado para que en produccion el mismo origen sirva front+API detras de un proxy - ESO todavia no existe en `docker-compose.yml` (sigue sin servicio `web`, ver M3 linea de Docker Compose); queda anotado como pendiente para cuando se arme el Dockerfile/nginx de `apps/web`.
  **Estructura creada**: `core/api/` (generado, 4 servicios: `DefaultService` (`/health`), `ExportsService`, `JobsService`, `OutputService`, mas `models/*` para cada schema del OpenAPI), `core/api-client.service.ts` (wrapper de inyeccion unica sobre los 4 servicios generados), `core/i18n/transloco-http-loader.ts`, `features/{upload,inventory,jobs,browser,settings}/` (cada una con `<feature>.routes.ts` + un componente de pagina placeholder "en construccion", todas standalone/OnPush/`inject()`-ready para la proxima ronda), `shared/coming-soon.ts` (componente reutilizable para los 5 placeholders). `app.routes.ts` usa `loadChildren` (no `loadComponent` directo) hacia cada `<feature>.routes.ts`, para que la estructura ya soporte que una feature crezca a mas de una ruta sin tocar `app.routes.ts` de nuevo.
  **Verificacion del cliente OpenAPI sin `any` "real"**: revisado archivo por archivo - los 5 endpoints con cuerpo JSON (`POST /exports`, `GET /exports/{id}/inventory`, `DELETE /exports/{id}`, `POST /exports/{id}/convert`, `GET /jobs/{id}`, `GET /exports/{id}/output/tree`) devuelven tipos generados fuertes (`ExportCreateResponse`, `Inventory`, `JobCreateResponse`, `JobStatusResponse`, `OutputTreeResponse`); los UNICOS 3 metodos sin tipo de retorno fuerte son exactamente los que spec 06 documenta como no-JSON (`GET /output/file` - contenido crudo con content-type variable, `GET /download` - zip binario, `GET /jobs/{id}/events` - `text/event-stream`), sin tipo generado posible por diseno del propio formato - la libreria de codegen los deja sobre HttpClient sin tipo de retorno fuerte, aceptado explicitamente por la consigna de esta ronda. La SSE de `/jobs/{id}/events` NO se va a consumir via este metodo generado (HttpClient no soporta streaming de SSE de forma nativa): la convencion documentada en `api-client.service.ts` es que la feature `jobs` envuelva `EventSource` directo en su propio servicio con una signal, como pide CLAUDE.md §7.
  **Tests**: `src/app/app.spec.ts` (2 tests: crea el shell, y verifica que el nav se renderiza en español con `TranslocoTestingModule` - reemplaza el smoke test generico "Hello, claude-export-md-web" del scaffold, que ya no aplica porque `app.html` se reemplazo por el layout real) + `src/app/core/i18n/i18n-parity.spec.ts` (2 tests, CA-3 de spec 07: compara TODAS las claves anidadas de `public/assets/i18n/es.json` contra `en.json` leyendo los archivos reales con `node:fs`, no un fixture inline - falla si una feature futura agrega una clave a un idioma y se olvida del otro). `pnpm test -- --watch=false`: 2 archivos, 4 tests, verde.
  **Lint**: `pnpm lint` (`ng lint` con `@angular-eslint/builder`) en verde. Regla `@typescript-eslint/no-explicit-any` subida a `error` explicito en `eslint.config.js` (viene como `warn` en `tseslint.configs.recommended`) para que "sin `any`" (CLAUDE.md §7) sea una regla real, no solo de estilo; `core/api/**` (generado) esta en `ignores` del lint - no se edita a mano y el propio generador no sigue estas convenciones (ver los `any` documentados arriba). Ademas hay un hook de proyecto (`.claude/hooks/guard_files.py`) que bloquea a nivel de `Write`/`Edit` cualquier `: any`/`<any>`/`as any` en `apps/web/src/**/*.ts` ANTES de que llegue a lint - lo dispara en el momento de escribir el archivo, no solo al correr `pnpm lint`.
  **Build**: `pnpm build` (`ng build`, configuracion `production` por defecto) en verde. Bundle inicial: **285.35 kB raw / 78.71 kB transferencia estimada (gzip)** - muy por debajo del limite de CA-6 (600 kB) y del budget configurado por el propio `ng new` (warning en 500 kB, error en 1 MB), con margen amplio para cuando las features reales (formularios, tablas de Material, visor Markdown) le sumen peso. Los 5 chunks lazy (uno por feature, ~450-2000 bytes cada uno hoy porque son placeholders) confirman que el lazy loading via `loadChildren` esta cableado correctamente desde esta ronda, no es una promesa para despues.
  **No se agregaron librerias fuera de las mencionadas en la consigna** (Angular Material, Tailwind 3, Transloco, openapi-typescript-codegen) salvo dependencias que esas mismas requieren para funcionar y no serian opcionales: `@angular/animations` (peer real de Material, punto 8 arriba), `@angular/cdk` (peer transitivo de Material, lo agrega el propio `ng add`), `@angular-eslint/*`/`eslint`/`typescript-eslint` (pedidos explicitamente por la consigna: "agrega ESLint... si `ng new` no lo trae por defecto" - no lo trae), `@types/node` (typings de `node:fs`/`node:path`/`node:url` usados SOLO en el test de paridad de i18n, no en codigo de produccion).
- [x] Fix previo: contrato OpenAPI completado (Problem en components.schemas, requestBody de creacion documentado, content-types reales de descarga/SSE/archivo) para que el cliente generado no salga con `any`. Queda un detalle cosmetico: FastAPI sigue agregando un content-type application/json generico junto al real en los endpoints binarios/SSE (no bloquea la generacion del cliente, el tipo correcto tambien esta presente)
- [ ] Feature upload - dropzone (zips/manifiesto), nota de privacidad, POST /exports
- [ ] Feature inventory - tabla por categoria (spec 07 pantalla 2), GET /inventory
- [x] Feature convert/progreso - POST /convert + SSE de progreso (spec 07 pantalla 3). `features/jobs/{convert.service.ts,job-progress.service.ts,event-source-factory.ts,convert-page.ts,convert-page.html}`. `ConvertService` envuelve `POST /exports/{id}/convert` (sin `only`/`templates`, CLAUDE.md 11) con las mismas signals `status/errorKey` que `UploadService`/`InventoryService`. `JobProgressService` envuelve `GET /jobs/{id}/events` con `EventSource` nativo detrás de `EventSourceFactory` (`providedIn: 'root'`, sustituible en tests - `EventSource` no existe en el entorno jsdom de Vitest) y expone `phase/events/summary/failureReason` como signals (CLAUDE.md 7). `ConvertPageComponent` arranca la conversion automaticamente en el constructor (mismo patron de `effect()` que `InventoryPageComponent` para su GET) y encadena un segundo `effect()` que conecta el SSE en cuanto el POST devuelve `job_id`. **Boton "Cancelar" (pendiente conocido de M3, sin endpoint de cancelacion)**: se implemento pero SOLO navega de vuelta a `/inventory/:exportId` sin llamar a la API - nunca simula una cancelacion que no existe; el texto de la propia UI (`jobs.actions.cancelHint`, es/en) aclara que la conversion sigue corriendo en el servidor. **CA-2 (SSE) - corte de conexion nunca se interpreta como fallo**: el evento nativo `error` de `EventSource` es ambiguo (mismo nombre para un corte de red real Y para el `event: error` que manda el propio servidor con `data` cuando el job termina en `failed`); se distinguen mirando si `event.data` es un string no vacio. Un corte real dispara `reconcileAfterConnectionLoss` (`GET /jobs/{id}` una unica vez) antes de decidir que mostrar: si el job ya esta `done`/`failed` del lado del servidor se refleja eso, si sigue `pending`/`running` se reconecta el stream solo, y si la propia reconciliacion falla por red recien ahi se ofrece un boton "Reintentar" manual. Tambien se guarda contra una visita directa a `/convert` sin `exportId` (el nav de `app.html` tiene un link fijo sin query param): `exportId` es `input()` opcional (no `input.required`), con un estado vacio dedicado en vez de que Angular tire `NG0950`. Tests: `convert-page.spec.ts` (4: flujo exitoso con progreso+resumen+navegacion a `/result/:exportId`, 409 con reintento, corte de conexion SSE reconciliado via GET, visita sin `exportId`) con un `FakeEventSourceFactory` que registra las instancias creadas para poder emitir `progress`/`done`/`error` a mano.
- [x] Feature resultado - arbol + visor + busqueda + descarga zip (spec 07 pantalla 4). `features/browser/{result-tree.ts,result.service.ts,result-page.ts,result-page.html}`. `result-tree.ts` (funciones puras, sin DI, con su propio spec) arma el arbol a partir de la lista PLANA de `GET /output/tree` (`buildOutputTree`, reconstruye carpetas intermedias por las que pasa un archivo aunque el backend no las liste explicitas), lo aplana para renderizar sin componentes recursivos (`flattenTree`, filas con `depth`) y lo filtra por nombre (`filterTree`, CA "busqueda por titulo": una carpeta se conserva entera si su propio nombre matchea, o solo con los descendientes que matchean si no). **`GET /output/file` NO se llama con el metodo generado** (`OutputService.getOutputFileApiV1ExportsExportIdOutputFileGet`, tipado `any`): el cliente generado (`core/api/core/request.ts`) no fija `responseType`, asi que `HttpClient` usa su default `'json'` e intenta `JSON.parse()` sobre la respuesta - que para un `.md`/texto plano real FALLA y el request sale como `HttpErrorResponse` pese a un 200 real del servidor (comportamiento documentado de Angular, confirmado leyendo `HttpXhrBackend`). `ResultService` llama a `HttpClient` DIRECTO con `responseType: 'text'` a la MISMA URL que usaria el cliente generado, evitando el bug sin tocar `apps/api` ni el generador. Extensiones binarias conocidas (imagenes/zips/fuentes/audio) se marcan "no previsualizable" SIN pedir el archivo al servidor (el export de `core` es texto de punta a punta - Markdown + artefactos de codigo -, asi que se usa una lista NEGRA de binarios en vez de una blanca de texto, mas robusta a extensiones de codigo no anticipadas). **Visor de Markdown SIN libreria nueva**: se muestra el contenido de cualquier archivo previsualizable (incluidos los `.md`) como texto preformateado (`<pre>`) en vez de agregar `ngx-markdown`/`marked`/similar - dado el criterio de la tarea ("si preferis no agregar ninguna dependencia esta ronda, mostralo como texto preformateado") y la politica del proyecto de justificar cada dependencia nueva (CLAUDE.md, unicas agregadas en M4 fueron peers reales de Material), se prioriza no sumar una libreria por una mejora puramente visual; queda anotado como mejora pendiente. **Descarga del zip con un `<a href>` plano** (`result-page.html`, `[href]="downloadHref()" download`) apuntando directo a `GET /exports/{id}/download`, no con el metodo generado (mismo bug de `responseType: 'json'` de arriba, agravado porque el body es un zip binario) ni con manejo manual de Blob - el enfoque mas simple y estandar para que el navegador dispare la descarga nativa, dejando que el `Content-Disposition: attachment` de `TempZipResponse` (spec 06) le de el nombre de archivo. `browser.routes.ts` gano `:exportId` como path param (antes solo `''`) mas un fallback `path: ''` que redirige a `/` (CA-2: el nav de `app.html` tiene un link fijo a `/result` sin id, que sin este fallback no matchearia ninguna ruta). Tests: `result-tree.spec.ts` (7, funciones puras) y `result-page.spec.ts` (4: arbol+archivo+link de descarga, error de red con reintento, filtro de busqueda, extension binaria sin pedirla al servidor).
- [ ] Revision de arquitectura + cierre de M4

## M5 - PyPI, plantillas personalizables, ejecutable

## Decisiones pendientes (para el usuario)
- [ ] Confirmar donde vive el CONTENIDO real de cada artefacto de frames (frames-000/artifacts/<uuid>/*.json solo trae metadata: id, kind, visibility, versions[] con title/description/created_at, pero ningun campo de tipo content/code/body). Ver docs/export-format/frames.md, seccion Dudas. Mientras tanto (M2) el parser conserva el JSON completo en Frame.payload y el frames/<slug>.md lo dice y lo publica entero, asi que el dia que se resuelva no habra que volver a leer el export.
- [ ] Confirmar donde vive el CONTENIDO de cada doc de un proyecto (projects-000/projects/<uuid>.json -> docs[] solo trae uuid/filename/created_at, sin el texto del doc). Ver docs/export-format/projects.md, seccion Dudas.
- [ ] Confirmar si memory_files[].path (dentro de memories-000/memories/<account_uuid>.json) codifica efectivamente rutas tipo "/profile.md" o "/people/nombre.md" - en el fixture anonimizado el valor esta reemplazado por longitud de cadena y no se puede leer. Ver docs/export-format/memories.md, seccion Dudas.
- [x] RESUELTO: el manifest_path=null de docs/export-format/inventory.json era porque el member-manifest-*.json no estaba todavia en la raiz del export cuando se corrio `just inventory` (el usuario lo ubico despues). No es un bug del detector de manifiesto. Documentado en docs/export-format/manifest.md. Hallazgo real: la clave raiz del manifiesto es `data_files` (no `files/exports/parts/data/items/artifacts` como se habia adivinado antes de la Fase 0). El usuario volvio a correr `just inventory` y el `inventory.json` local ya muestra manifest_path resuelto y missing_categories/missing_parts vacios - ese archivo NO se commitea (esta en .gitignore a proposito, es un artefacto local de exploracion, no un entregable versionado).
- [x] adapters/manifest.py no reconocia la clave raiz real `data_files` del manifiesto (ver arriba) - RESUELTO, `data_files` es ahora la primera clave probada en `_LIST_KEYS`, con test de regresion contra el fixture real.
- [ ] Se publica en PyPI bajo nombre claude-export-md o con prefijo propio?

## Deuda tecnica (revision architect-reviewer, cierre de M0)
- [ ] usecases/inventory.py: si falla la lectura del manifiesto, el nombre real del archivo (con uuid de cuenta) queda sin redactar en `warnings` -> aplicar `redact_text()` al mensaje de la excepcion antes de loguearlo/guardarlo.
- [ ] adapters/manifest.py: el log de `path.name` cuando falla el manifiesto tambien va crudo -> redactar igual que arriba.
- [ ] usecases/inventory.py: los nombres de categoria que vienen del manifiesto (declared_parts) pueden terminar sin redactar en warnings/missing_parts si algun dia una categoria no reconocida trae texto libre - aplicar redact_text() al consumir declared_parts().
- [ ] docs/specs/02-domain-model.md: `Memory` (`path, content, updated_at?, extra`) no tiene campo para asociarse a un Project, pero 03-parsers.md (CA de memories) exige que quede "ligada a ese Project por uuid" via `project_memories`. Contradiccion real a resolver en M1 con un ADR antes de tocar el modelo (spec 02 CA-5 exige ADR para cambiar la lista de entidades) - probablemente `Memory` necesite `project_id: str | None`.
- [ ] El fixture real de v2026-batched/conversations/ no lo usa ningun test todavia - es documentacion, no red de seguridad; atarlo a un test como ya estan los demas (manifest desde M0, memories desde M1, y desde M2 light_metadata via test_parse_light_metadata.py::test_the_real_anonymised_export_shape_parses, projects via test_parse_projects.py y frames via test_parse_frames.py::test_the_real_anonymised_export_shape_parses).
- [ ] CLAUDE.md seccion 5 sigue describiendo la SALIDA de memories como `memories/profile.md`, `people/*.md` heredando la hipotesis (falsa) de que la ENTRADA es una carpeta de .md. La salida en carpetas sigue siendo valida, pero el nombre de cada archivo lo decide la herramienta slugificando `Memory.path` (`/people/Ana María.md` -> `memories/people/ana-maria.md`) y aparecen dos carpetas que no existen en Claude.ai: `memories/conversations-memory.md` y `memories/project-memories/<uuid>.md` (ADR-0005). Conviene que el usuario lo refleje en CLAUDE.md seccion 5; documentado mientras tanto en spec 04, seccion Plantillas.
- [ ] `--keep-structure-only` de anonymize_fixture.py rompe integridad referencial entre fixtures (un uuid como valor -> `<str:N>`, como clave -> uuid falso determinista) - para M1 conviene una variante que anonimice valores con forma de uuid/email de forma deterministas (no solo claves), asi se puede probar `conversation.project_uuid -> project.uuid` con datos reales anonimizados.
- [x] RESUELTO en M4 (scaffold): apps/web dejo de ser un stub (scripts/noop-*.cjs borrados); `pnpm -C apps/web lint`/`test`/`build` ahora ejercitan una app Angular 21 real. `just lint`/`just test` de la raiz siguen sin cambios de comando (ya llamaban a `pnpm -C apps/web lint`/`test`), simplemente dejan de ser no-ops.
- [ ] CI (.github/workflows/ci.yml) nunca corrio en GitHub Actions todavia, solo se verifico el equivalente local en Windows.
