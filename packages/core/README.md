# claude_export_md (core)

Librería Python pura que convierte una exportación de datos de Claude.ai en una
carpeta navegable de archivos Markdown. Sin dependencias de HTTP, CLI ni framework
(ver `CLAUDE.md` §1 en la raíz del repo).

API pública en `claude_export_md/__init__.py`.
