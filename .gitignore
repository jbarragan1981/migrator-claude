# Datos reales del usuario — NUNCA al repo
exports/
output/
*.zip
member-manifest-*.json
# Inventario generado sobre el export real: el core ya lo redacta (spec 01), pero
# no se commitea nunca (red de seguridad, no la protección principal).
docs/export-format/inventory.json
.env
.env.*
# Python
.venv/
__pycache__/
*.pyc
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
dist/
# Node
node_modules/
apps/web/.angular/
apps/web/dist/
# Claude Code (local, no compartido)
.claude/settings.local.json
